# {{题库名称}} — 报告生成提示词

> ⚠️ 这是一个模板文件。复制 `_template/` 文件夹后，按下方说明填写。
>
> ## 使用说明
>
> 1. 将 `_template/` 复制为新题库的文件夹名（如 `my-survey/`）
> 2. 删除此说明块，保留下方提示词正文
> 3. 将 `{{PLACEHOLDER}}` 替换为你的动态数据占位符
> 4. 在对应的 ReportService 中实现占位符 → 值的映射
>
> ## 占位符命名规范
>
> - 使用大写蛇形命名：`{{USER_NAME}}`、`{{TOTAL_SCORE}}`
> - 迭代数据预格式化后注入：`{{DIMENSION_SCORES_TABLE}}`
> - 条件段落预计算后注入：`{{WARNINGS_SECTION}}`
> - 参考 `mids-f2/prompt.md` 作为完整示例
>
> ## 必填项
>
> - [ ] AI 角色设定（第一段）
> - [ ] 输出格式要求（JSON / [SECTION:xxx] / 其他）
> - [ ] 风格约束
> - [ ] 占位符清单（所有注入数据的 {{NAME}}）

---

你是一位[填写：AI 角色身份]。请根据以下测评数据，生成一份专业、个性化的测评解读报告。

## 输出要求
[填写：输出格式，如 JSON Schema、[SECTION:xxx] 标记分割等]

## 报告风格约束
1. [填写：风格要求]
2. [填写：用词规范]
3. [填写：数据引用要求]

## 测评数据

测评对象：{{USER_NAME}}
[填写其他动态数据，使用 {{PLACEHOLDER}}]

请现在输出[格式要求]，不要加任何前缀或后缀。
